import React from 'react';

// contains messages for the user

export const MessageLoading = (
    <div className="msg">Loading...</div>
);

export const MessageSaving = (
    <div className="msg">Saving...</div>
);

export const MessageActivityNotFound = (
    <div className="msg">Activity not found</div>
);